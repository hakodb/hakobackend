# driver.example.toml — new database addon manifest template.
# Copy to crates/hakobackend-db-<name>/driver.toml then fill in. See DRIVER_CONTRACT.md.
[driver]
name = "replace-driver-name"
version = "0.1.0"
description = "Short description of the wrapped backend"

[capabilities]
watch = false
transactions = false

[config]
# Example database.path value for this driver:
path = "example-dsn-or-path"
