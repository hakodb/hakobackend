[package]
name = "hakobackend-policy"
version = "0.1.0"
edition = "2021"

[dependencies]
hakobackend-core = { path = "../hakobackend-core" }
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }

[dev-dependencies]
# (empty — serde_json is already a main dependency since roles_of uses it)
